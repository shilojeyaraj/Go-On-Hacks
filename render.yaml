services:
  - type: web
    name: toegether-app
    env: node
    buildCommand: npm run install:all && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production

